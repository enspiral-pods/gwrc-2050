import { Flex } from 'rebass'
import styled from 'styled-components'
import {
  display,
  position,
  height,
  maxWidth,
  minWidth,
  maxHeight,
  minHeight,
  top,
  right,
  bottom,
  left,
  borders,
  zIndex,
  textAlign
} from 'styled-system'

export default styled(Flex)(
  display,
  position,
  height,
  maxWidth,
  minWidth,
  maxHeight,
  minHeight,
  top,
  right,
  bottom,
  left,
  borders,
  zIndex,
  textAlign
)
